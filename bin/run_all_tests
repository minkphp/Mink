#!/bin/sh

bin/start_sahi
bin/start_selenium
# don't need to start "webdriver" since selenium will do the job!
#bin/start_webdriver

phpunit

bin/kill_selenium
bin/kill_sahi
# don't need to start "webdriver" since selenium will do the job!
#bin/kill_webdriver
